# vibe-diagram reference: feature-iteration

> 本文件由 TASK_20260630_022 拆分自 `vibe-diagram/SKILL.md`。只有路由命中对应图型后才读取；读取失败必须 fail-closed。


## 候选全集清单

- 生图类型：功能迭代 / 开发设计图
- 首选候选：当前流程 vs 目标流程的流程化对照
- 必生成备选候选：
  - current/target 技术时序
  - 差异热区
  - 发布回滚轨道
- 校准期要求：当前图型命中后，HTML 内必须按候选 A/B/C/D…纵向展开首选候选与全部备选候选；每个候选都必须是真图。信息不足时也必须生成该备选候选，用“待确认节点”标明缺口。

### HTML 模板资产

本图型必须优先复制 HTML 模板骨架，再替换槽位；禁止从零自由绘制整体布局。模板目录：`templates/feature-iteration/`。模板只固定视觉语法、关系主轴、节点居中和响应式结构，不固定业务内容。

使用流程：

1. 先按用户真实问题选择当前图型内的主模板。
2. 从 `templates/feature-iteration/` 复制最匹配的 HTML 文件。
3. 只替换 `data-slot` 槽位内容、标题、摘要和必要节点文本。
4. 保留 `data-diagram-type="feature-iteration"`、`data-template-family="feature-iteration"`、`data-template-id="..."`、`data-template-layout="..."`、主画布结构、节点居中 CSS 和响应式 CSS。
5. 如果模板槽位不够，先合并/隐藏/复制同类槽位；仍不够时才新建局部子结构，不能破坏主骨架。
6. 同一图型内的模板不能共享同一宏观骨架或同一 DOM 骨架；`data-template-layout` 必须体现该模板的版式语义，主画布结构要能在去掉文案和槽位名后仍可区分，避免退化成同一种 grid/card 预览。

模板清单：

| 模板文件 | data-template-layout | 用途 |
| --- | --- | --- |
| `current-target-flow.html` | `current-target-flow` | 当前流程 vs 目标流程的流程化对照 |
| `current-target-sequence.html` | `current-target-sequence` | current/target 技术时序 |
| `diff-heatmap.html` | `diff-heatmap` | 差异热区 |
| `release-rollback-track.html` | `release-rollback-track` | 发布回滚轨道 |

## 功能迭代 / 开发设计图专用骨架

功能迭代图必须先画当前功能和当前实现，再画目标和差异。用户需要先理解“现在产品怎么用、代码怎么实现”，再判断迭代方案是否安全、完整、可回滚；不得只画目标方案。

固定主骨架：

`当前功能主路径 → 当前开发实现链路 → 目标主路径 → 差异映射 → 验证与发布闭环`

骨架执行规则：

1. **当前功能主路径**：当前功能主路径必须说明用户现在入口、动作、状态和结果；如果当前体验有多端差异，先分端或标注统一口径。
2. **当前开发实现链路**：当前开发实现链路必须说明前端入口、API、服务、DB/状态、中间件或异步任务；不知道的节点写“待确认”，不能脑补成确定事实。
3. **目标主路径**：目标主路径只画用户改完后最重要的动作和结果，避免把需求清单平铺成多个模块。
4. **差异映射**：差异映射必须把新增 / 修改 / 删除 / 不变 / 风险 / 回滚贴到对应节点；每个变更点都要能追溯到页面、接口、模块、数据或规则之一。
5. **验证与发布闭环**：AC、测试矩阵、灰度发布、监控和回滚动作必须写入或贴近差异节点；不要把验收标准和测试表丢到最底部独立堆放。

按迭代性质选择形态：

- 体验变更用 before/after 用户主路径对照：左/上为当前体验，右/下为目标体验。
- 技术链路变更用 current/target 技术时序或数据流对照：当前接口、服务、DB/状态与目标链路一一映射。
- 大迭代用用户体验层 → 系统实现层 → 验证发布层：三层纵向卷轴表达从用户价值到落地发布的递进关系。

## 根因、修法与前后对照规则

根因和修复方案不是说明文字，必须成为主图中的视觉焦点。

1. 缺陷排查和开发设计默认使用单图高亮根因与修法：根因节点标 `根因 R#`，计划修法节点标 `修法 F#`，验证节点标 `验证 V#`
   ；不能只靠颜色表达。
2. 证据必须贴近或连到根因节点，测试与回滚必须贴近或连到修法节点，避免证据区、方案区、风险区各自漂浮。
3. 当修复改变两处以上关键节点，或改变调用链、数据流、状态流转、接口契约、权限边界、用户主路径之一时，必须改用前后对照图。
4. 前后对照图必须明确：左/上为当前逻辑或故障逻辑，右/下为修复后逻辑或目标逻辑，中间或旁路标出保留、删除、新增、风险和回滚点。
5. 若前后差异很小，禁止为了形式感画双图；用单图高亮根因与修法更清晰。
6. 若前后双图在桌面宽度内仍会拥挤，必须改为上下两图或纵向阶段对照，不得缩小字体或继续横向加卡片。
7. before 固定在左侧或上方，after 固定在右侧或下方；这是前后对照图的阅读约定，不能随节点编号或美观需要调换。
8. 禁止用左右交替排布表达前后差异；左右交替只能用于非对照型时间线，且不得与 before / after 语义混用。
9. 纵向卷轴中也必须保持左侧为当前/故障逻辑，右侧为修复后/目标逻辑；移动端折叠时保持上方为 before、下方为 after。
10. 如果节点不属于前后对照，只能放在中轴、旁注或详情中，避免把中性说明误读为 before 或 after。

## 流程图语法门禁

前后对照只是容器，不是图形语法本身。功能迭代、开发设计和故障修复必须优先画流程图或流程化对照图。

1. 主画布必须包含开始/结束事件、活动节点、决策菱形、带标签箭头；缺少这些流程图基本元素时，不得把结果称为流程图。
2. before/after 每一侧内部也必须是流程图：当前逻辑要展示旧流程如何走到问题点，修复后逻辑要展示新流程如何绕过或消除问题点。
3. 禁止把 before/after 列画成普通说明卡片列表；每一列至少要有 3 个按方向连接的流程节点，且必须有一个明确输入和一个明确输出。
4. 根因节点和修法节点必须落在流程路径上，不能浮在旁边当说明卡片；根因节点用 `根因 R#`，修法节点用 `修法 F#`。
5. 辅助证据优先写入流程节点内部，或作为流程节点的证据锚点 / 紧邻旁注；原始长材料可放点击详情，但不得把证据列表、假设列表、方案列表画成与流程同等重量的主内容。
6. 对开发迭代图，必须画出用户/系统当前主流程、变更点、目标主流程、验证闭环；对故障修复图，必须画出排查流程、判断分支、根因定位、修复动作、验证/回滚闭环。
7. 若流程跨角色或跨端，使用泳道或阶段轨道；若流程跨状态，使用状态流转；若流程跨接口/数据，使用时序或数据流。不要用卡片网格替代这些语法。
