# TASK_20260701_010 全类型候选标题去前缀规则

## 背景

用户在验收 HTML 中指出：按钮和面板标题不需要 `候选 A`、`验收视图`、`验收图层` 等前缀；这些表达像未完成方案，而不是已完成交付的图名。

## 变更范围

- `vibego_cli/data/skills/vibe-diagram/SKILL.md`：在“候选全集校准模式”中新增全类型规则：候选切换按钮和候选面板可见标题只显示图名本身；禁止
  `候选 A：`、`方案 A：`、`验收视图：`、`验收图层：` 等前缀；内部 DOM id/hash/aria-controls 仍可使用稳定编号；推荐、首选、适用边界等说明只能放辅助文案或说明栏。
- `tests/test_builtin_skills_injection.py`：新增
  `test_vibe_diagram_candidate_labels_must_use_plain_diagram_names_for_all_types` 固化规则。
- `AGENTS.md`：更新事实表中“多方案/多候选全类型适用边界”的证据和规则口径。
- `/Users/david/.config/vibego/agents/current/vibego_cli/data/skills/vibe-diagram/SKILL.md`：已通过 `agents-sync` 同步本机活跃
  skill。

## TDD 记录

1. RED：先新增 `test_vibe_diagram_candidate_labels_must_use_plain_diagram_names_for_all_types`。
2. 失败命令：
   `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_candidate_labels_must_use_plain_diagram_names_for_all_types`。
3. 失败原因：`vibe-diagram/SKILL.md` 未包含“可见标题只显示图名本身”的全类型规则。
4. GREEN：在 `vibe-diagram/SKILL.md` 共性候选全集规则中新增该约束，并补输出前自检。
5. 通过命令：同一聚焦测试 → `1 passed in 0.01s`。

## 验证

| 命令                                                                                                                                              | 结果                                | 说明                                                                                            |
|-------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|-----------------------------------------------------------------------------------------------|
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_candidate_labels_must_use_plain_diagram_names_for_all_types` | `1 passed in 0.01s`               | 新规则聚焦测试通过                                                                                     |
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py`                                                                                | `54 passed in 0.08s`              | 内置 skill 相关回归通过                                                                               |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                     | `ok: true`，4 个 target 为 `updated` | 活跃 skill 与全局索引已同步                                                                             |
| `rg` 检查活跃 skill                                                                                                                                 | 命中新增规则                            | `/Users/david/.config/vibego/agents/current/vibego_cli/data/skills/vibe-diagram/SKILL.md` 已同步 |

## 影响与回滚

- 影响入口：后续所有 `vibe-diagram` 生图类型的候选按钮和候选面板标题写法。
- 用户待执行：本轮已同步；无需再执行同步脚本。已运行长期 worker 如需立刻使用新 skill，请按现有流程重启。
- 回滚：删除 `vibe-diagram/SKILL.md` 中新增候选标题规则与自检项，删除新增测试断言，并重新执行 `agents-sync`。
