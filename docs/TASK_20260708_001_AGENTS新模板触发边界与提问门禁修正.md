# TASK_20260708_001 AGENTS 新模板触发边界与提问门禁修正

## 背景

用户复核 `<feat> 调整提示词` 后指出三类问题：

1. `AGENTS.md` 事实表仍停留在旧模板口径，需要按当前新模板更新。
2. `vibe-diagram / HTML` 触发边界需要判断是否只是提示词精简，还是实质收窄。
3. Readiness Gate 之前的交互不稳定：仍可能一次问 2-3 个独立问题，且没有稳定优先通过提问确认用户意图、真实需求再进入设计。

## 取证结论

- 对比 `1adb35b8..72f97762`，`vibe-diagram / HTML` 不是单纯精简，而是删除了多个旧触发场景：具体对象/代码/文件更新/diff
  解释、行为/故障为什么、系统/业务/流程/时序/需求决策视觉沟通、完整逻辑/数据口径/根因链路、以及“生成或更新项目内单文件
  HTML / HTML 是主交互界面”。
- 当前模板虽然有 `Readiness Gate`，但主要写成“进入 develop 前”的清单；缺少“未达到 95%
  前唯一允许动作”的阻断式交互循环，因此模型容易先输出设计/计划，或一次性打包多个澄清问题。

## 设计决策

- 不恢复旧长提示词，采用“短模板 + 紧凑触发矩阵”。
- 新增 `Interaction Gate`，把 Readiness 未达标状态显式标记为 `READINESS: BLOCKED`。
- Readiness 未达标前，只允许只读取证、列已确认事实、列阻塞未知，并一次只问一个最高影响问题；不得输出完整设计、实现计划、任务拆解或最终方案。
- Product Design 恢复条件路由和安全边界：环境提供才按需使用、不会自动安装、缺失回退、必须先确认 brief、无视觉目标先 3
  个方向等待选择。

## 变更点

- `AGENTS-template.md`
    - `Readiness Gate` 前移到进入设计、计划或 develop 前。
    - 新增 `Interaction Gate`。
    - 恢复紧凑版 `vibe-diagram / HTML` 触发矩阵。
    - 恢复 Product Design 条件路由、brief gate 和前端实现边界。
- `AGENTS.md`
    - 更新 Facts Table 中 AGENTS 模板协作内核、具体对象解释/行为故障为什么、HTML 图形沟通、Product Design、HTML 交付后文本压缩等事实。
- `tests/test_agents_template_migration.py`
    - 新增事实表同步断言。
    - 将旧“移除 brainstorming 阶段门禁”测试改为 `Interaction Gate` 行为断言。
    - 更新可选评论断言为中文口径。

## TDD 记录

- RED：`python3.11 -m pytest -q tests/test_agents_template_migration.py`
    - 结果：`9 failed, 8 passed`。
    - 失败点覆盖 `AGENTS.md` 事实表、`Interaction Gate`、Product Design 门禁、运行入口去耦、vibe-diagram/HTML 触发边界和
      HTML 交付瘦身。
- GREEN：`python3.11 -m pytest -q tests/test_agents_template_migration.py`
    - 结果：`17 passed in 0.03s`。

## 验证记录

- `python3.11 -m pytest -q tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`6 passed in 0.04s`。
- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`23 passed in 0.05s`。
-
`git diff --check -- AGENTS-template.md AGENTS.md tests/test_agents_template_migration.py docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；已同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。

## 二次修正（2026-07-08）

用户追问后确认：上一版确实把旧 `vibe-diagram` 相关触发文案大部分恢复，仍有冗余。已改为单一 `Vibe / HTML trigger matrix`
，把触发条件、HTML 承载和聊天信封集中到一个矩阵中，删除 `Skill routing`、`HTML / visual delivery contract`、
`Visual and frontend contract` 三处重复表达。

用户同时要求删除提示词模板测试用例。本轮按安全范围处理：删除 `tests/test_agents_template_migration.py` 中纯
`AGENTS-template.md` 文案断言，保留模板文件名迁移、打包清单、README 链接和 Telegram 前缀注入等真实行为测试，避免削弱非提示词文案类长期验证资产。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.06s`。
-
`git diff --check -- AGENTS-template.md AGENTS.md tests/test_agents_template_migration.py docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；随后 `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md` 输出
      `active template synced`。

## 三次修正（2026-07-08）

用户确认继续按渐进式披露优化后，进一步压缩 `AGENTS-template.md`：

- Product Design 只保留路由条件和“遵守其 brief/visual-target 门禁”，不在全局模板重复展开 3 方向、文字 brief 等细节。
- `Vibe / HTML trigger matrix` 压缩为“触发 / 不触发 / 命中后交付”三行，具体制图规则继续由 `vibe-diagram` skill 和
  references 按需承载。
- 前端契约中移除 Product Design 作为所有前端细节默认依据，避免与按需路由冲突。
- `AGENTS.md` 事实表同步移除已删除的纯模板文案测试锚点，避免引用不存在的测试资产。

验证：

- 保留用户要求：不新增纯提示词模板断言测试。
- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.05s`。
-
`git diff --check -- AGENTS-template.md AGENTS.md tests/test_agents_template_migration.py docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；随后 `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md` 输出
      `active template synced`。

## 四次修正（2026-07-09）

用户指出 superpowers 已有 review skill，要求在 `Skill routing` 中固化开发完成后的 review 门禁，并明确 review 必须遵守项目
`AGENTS.md`。本轮按既有“不要新增纯提示词模板断言测试”边界处理：

- `AGENTS-template.md`：在 `superpowers:test-driven-development` 与 `superpowers:verification-before-completion` 之间新增
  `superpowers:requesting-code-review` 门禁。
- `AGENTS.md`：同步事实表，记录开发任务完成后必须 review，且 review 请求必须要求 reviewer 读取并遵守当前项目 `AGENTS.md`
  及相关上级规约。
- `/Users/david/.config/vibego/agents/current/AGENTS-template.md`：通过 `agents-sync` 同步，保持活跃模板与仓库模板一致。
- 测试策略：不新增纯提示词模板断言；用现有模板迁移测试、diff hygiene、agents-sync JSON 和模板一致性检查验证。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.10s`。
- `git diff --check -- AGENTS-template.md AGENTS.md docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`
    - 结果：`active template synced`。
- 内容抽查：
  `grep -n "requesting-code-review\|读取且遵守当前项目 AGENTS.md" AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：四处均包含新增 review 门禁。

## 五次修正（2026-07-09）

用户确认其常在没有 `.git` 的父级目录工作，下级可能存在多个 Git 子仓。上一版 review 门禁只要求使用
`superpowers:requesting-code-review` 和遵守 `AGENTS.md`，但 `superpowers:requesting-code-review` 默认从单仓
`git rev-parse` / `git diff BASE..HEAD` 取证，无法天然覆盖无 Git 父目录或多个子 Git 仓。

本轮补充多 Git 工作区规则：

- `AGENTS-template.md`：review 请求必须要求 reviewer 同时读取当前项目、受影响子仓和相关上级规约。
- `AGENTS-template.md`：开发、review 和 verification 前必须先定位真实 Git 工作区；若当前目录不是 Git 仓，或父级目录下存在多个子
  Git 仓/子模块，必须逐个受影响子仓收集 cwd、branch、status、diff/暂存 diff 和验证命令。
- `AGENTS-template.md`：禁止只用父目录 `git status`、单一 SHA 区间或聚合仓 clean 状态下结论。
- `AGENTS.md`：同步事实表，记录多子仓 review/verification 的取证约束。
- 测试策略：延续用户此前要求，不新增纯提示词模板断言；通过现有迁移测试、diff hygiene、同步命令和内容抽查验证。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.07s`。
- `git diff --check -- AGENTS-template.md AGENTS.md docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`
    - 结果：`active template synced`。
- 内容抽查：
  `grep -n "真实 Git 工作区\|各受影响子仓 AGENTS.md\|父目录的 git status" AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：四处均包含新增多 Git 工作区取证门禁。

## 六次修正（2026-07-09）

用户指出：不要把触发条件显示成堆积关键词，应抽象提取“代码逻辑解释未触发图”的场景后再修改提示词。

本轮将 `Vibe / HTML trigger matrix` 从对象/关键词枚举改为抽象判断标准：

- 如果回答需要说明一个实现对象如何在系统中产生、流转、转换并影响结果，必须使用 `vibe-diagram`。
- 如果回答需要呈现跨角色/模块的因果、时序、状态、数据、证据、前后差异或验收关系，必须使用 `vibe-diagram`。
- 如果纯文本会退化成路径、函数、字段或证据 bullet 清单，也必须改成图。
- 只有一个短句即可讲清的定义、翻译、简单命令、安装升级、轻量取舍或用户明确不要图时，才默认简洁文本。

同步更新：`AGENTS-template.md`、`AGENTS.md` Facts Table、`README.md` 触发边界、
`tests/test_vibe_diagram_plugin_distribution.py` README 断言，以及 `docs/TASK_20260709_001_代码逻辑解释未触发HTML图排查.html`
，避免排查页自身继续堆关键词。

验证：

- RED：`python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`1 failed, 10 passed`；失败点是 README 测试仍断言旧“复杂技术/业务逻辑、关系结构或状态流转可视化”等关键词清单。
- 聚焦 GREEN：
  `python3.11 -m pytest -q tests/test_vibe_diagram_plugin_distribution.py::test_readme_documents_vibe_diagram_distribution_and_trigger_scope`
    - 结果：`1 passed in 0.01s`。
- 相关回归：
  `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.06s`。
-
`git diff --check -- AGENTS-template.md AGENTS.md README.md tests/test_vibe_diagram_plugin_distribution.py docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md docs/TASK_20260709_001_代码逻辑解释未触发HTML图排查.html`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`
    - 结果：`active template synced`。
- HTML 自检：确认 `docs/TASK_20260709_001_代码逻辑解释未触发HTML图排查.html` 无外部资源、保留 `<!doctype html>`
  ，且不再包含“解释代码逻辑/字段用途/Token/header 传递链路”等关键词堆积表达。
- 内容抽查：
  `grep -n "一个实现对象如何在系统中产生\|纯文本会退化成路径\|不要把关键词堆成触发条件" AGENTS-template.md README.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：仓库模板、README、活跃模板、Codex AGENTS 与 vibego AGENTS 均包含抽象触发规则。

## 七次修正（2026-07-09）

用户确认：选择 Subagent-Driven 后，subagent 应保持与主会话的模型与推理强度一致，不应因为 superpowers skill
中“省成本/速度”的默认建议降级到低级模型，也不应改为读取配置文件替代当前会话设置。

本轮补充 subagent 模型继承规则：

- `AGENTS-template.md`：使用 subagent（含 Subagent-Driven、reviewer、fixer）时，默认不得显式指定 `model` 或
  `reasoning_effort`，必须继承主会话当前模型与推理强度。
- 仅在用户明确要求切换，或任务失败后需要升级并获得用户确认时，才允许覆盖模型/推理强度。
- 明确禁止为省成本/速度降级到低级模型，也禁止读取配置文件替代当前会话设置。
- `AGENTS.md`：同步 Facts Table，记录 subagent 模型继承门禁。
- 测试策略：延续用户此前要求，不新增纯提示词模板断言；通过现有回归测试、diff hygiene、agents-sync 和内容抽查验证。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.06s`。
-
`git diff --check -- AGENTS-template.md AGENTS.md README.md tests/test_vibe_diagram_plugin_distribution.py docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md docs/TASK_20260709_001_代码逻辑解释未触发HTML图排查.html`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`
    - 结果：`active template synced`。
- 内容抽查：
  `grep -n "主会话当前模型与推理强度\|不得为省成本/速度降级\|reasoning_effort" AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：仓库模板、活跃模板、Codex AGENTS 与 vibego AGENTS 均包含 subagent 模型继承规则。

## 八次修正（2026-07-09，已被九次修正收窄）

用户指出今天出现过“代码完全没看到”的现场，决定先默认禁止自动使用 worktree / 隔离工作区。

后续用户进一步判断：一般只有 `superpowers:using-git-worktrees` 喜欢主动创建 worktree，因此不应把限制放进全局
`AGENTS-template.md`，而应直接限制对应 skill。见“九次修正”。

本轮补充 worktree 默认禁用规则：

- `AGENTS-template.md`：默认禁止自动创建、切换或要求使用 git worktree / Codex native worktree / 其他隔离工作区。
- 只有用户在本轮明确要求或确认后，才允许创建或进入 worktree / 隔离目录。
- 若相关 skill 建议 worktree，未获许可时只能做只读检查，并在当前工作区继续。
- 开发、review 和 verification 前若未获 worktree 许可，必须先确认 cwd、git root、关键源码存在和 git status，避免模型进入看不到代码的错误现场。
- `AGENTS.md`：同步 Facts Table，记录 worktree 默认禁用门禁。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.06s`。
- `git diff --check -- AGENTS-template.md AGENTS.md docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`
    - 结果：`active template synced`。
- 内容抽查：
  `grep -n "默认禁止自动创建、切换或要求使用 git worktree\|未获用户本轮明确许可" AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：仓库模板、活跃模板、Codex AGENTS 与 vibego AGENTS 均包含 worktree 默认禁用规则。

## 九次修正（2026-07-09，已被十次修正撤回）

用户确认：worktree 默认禁用不应作为全局 AGENTS 规则，而应收窄到对应 skill。

后续用户指出：这里的“对应 skill”不是修改插件原始 `SKILL.md`，而是在 `AGENTS-template.md` 的 Skill routing 中对
`superpowers:using-git-worktrees` 的使用方式加限制。见“十次修正”。

本轮调整：

- 撤回 `AGENTS-template.md` 中新增的全局 worktree 禁令，避免影响非 superpowers / 非 worktree 场景。
- 撤回 `AGENTS.md` Facts Table 中关于全局 worktree 默认禁用的描述。
- 直接修改两个有效 skill 副本：
    - `/Users/david/.codex/plugins/cache/openai-curated-remote/superpowers/6.1.1/skills/using-git-worktrees/SKILL.md`
    - `/Users/david/.agents/skills/using-git-worktrees/SKILL.md`
- 新 skill 规则：默认在当前工作区继续；只有用户在当前任务明确要求或确认 worktree / 隔离工作区时，才允许创建或切换。
- 未获当前任务显式 opt-in 时，不得仅因 feature work 或 implementation plan 开始就主动询问 worktree；必须先验证 `pwd`、git
  root、关键源码可见性和 git status。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.06s`。
- `git diff --check -- AGENTS-template.md AGENTS.md docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`
    - 结果：`active template synced`。
- 全局撤回抽查：
  `grep -RIn "默认禁止自动创建、切换或要求使用 git worktree\|未获用户本轮明确许可" AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：无匹配；全局 AGENTS 不再包含 worktree 禁令。
- skill 内容抽查：
  `grep -n "Default: No Worktree Without Current-Task Opt-In\|without explicit current-task user opt-in" /Users/david/.codex/plugins/cache/openai-curated-remote/superpowers/6.1.1/skills/using-git-worktrees/SKILL.md /Users/david/.agents/skills/using-git-worktrees/SKILL.md`
    - 结果：两个 skill 副本均包含默认不创建、不询问 worktree 的局部限制。

## 十次修正（2026-07-09）

用户纠正：不应修改 `/Users/david/.codex/plugins/.../SKILL.md` 或 `/Users/david/.agents/skills/.../SKILL.md` 原始 skill
文件，而应在 `AGENTS-template.md` 的 `## Skill routing` 中约束对应 superpowers skill 的使用方式；subagent 模型继承规则也应直接关联到会派发
subagent 的 superpowers skill。

本轮调整：

- 恢复两个原始 `using-git-worktrees/SKILL.md` 文件，撤回对插件/本机 skill 本体的直接修改。
- `AGENTS-template.md` 的 `## Skill routing` 新增 `superpowers:using-git-worktrees` 使用限制：
    - 默认只做现有隔离状态与当前工作区可见性检查。
    - 不得仅因 feature work、implementation plan 或该 skill 自身建议而主动询问、创建、切换 worktree / 隔离工作区。
    - 只有用户本轮明确要求或确认 worktree / 隔离目录时才允许创建或进入。
    - 否则必须在当前工作区确认 cwd、git root、关键源码存在和 git status 后继续。
- `AGENTS-template.md` 的 subagent 模型继承规则改为直接关联：
    - `superpowers:subagent-driven-development`
    - `superpowers:requesting-code-review`
    - 任何会派发 subagent/reviewer/fixer 的 skill
- `AGENTS.md` Facts Table 同步更新上述 Skill routing 事实。

验证：

- `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_vibe_diagram_plugin_distribution.py`
    - 结果：`11 passed in 0.06s`。
- `git diff --check -- AGENTS-template.md AGENTS.md docs/TASK_20260708_001_AGENTS新模板触发边界与提问门禁修正.md`
    - 结果：exit 0。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`
    - 结果：`ok: true`；同步 `/Users/david/.config/vibego/agents/current/AGENTS-template.md`、
      `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
      `/Users/david/.config/vibego/AGENTS.md` 和 native skill 目标。
- 内容抽查：
  `grep -n "superpowers:using-git-worktrees\|superpowers:subagent-driven-development" AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md /Users/david/.codex/AGENTS.md /Users/david/.config/vibego/AGENTS.md`
    - 结果：仓库模板、活跃模板、Codex AGENTS 与 vibego AGENTS 均包含 Skill routing 绑定规则。
- 原始 skill 恢复抽查：
  `grep -n "Would you like me to set up an isolated worktree" /Users/david/.codex/plugins/cache/openai-curated-remote/superpowers/6.1.1/skills/using-git-worktrees/SKILL.md /Users/david/.agents/skills/using-git-worktrees/SKILL.md`
    - 结果：两个原始 skill 文件均恢复为原文询问语句，且不再包含 `Default: No Worktree Without Current-Task Opt-In` 自定义块。

## 风险与回滚

- 风险：模板再次变长，可能增加常驻提示词 token；已通过“紧凑矩阵”避免恢复旧大段解释。
- 风险：`Interaction Gate` 可能增加对话轮次；这是用户明确期望的 Readiness 前置交互行为。
- 回滚：恢复 `AGENTS-template.md`、`AGENTS.md`、`tests/test_agents_template_migration.py` 和本文档到本任务前状态。
