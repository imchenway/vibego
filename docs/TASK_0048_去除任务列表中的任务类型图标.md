# /TASK_0048 去除任务列表中的任务类型图标

## 0. 摘要
- 目标：在“任务列表/任务搜索结果”的任务按钮中，去除“任务类型 emoji 图标”（如 📌🐞🛠️⚠️/⚪），仅保留必要的信息（如状态图标与标题）。

---

## 1) vibe 阶段（需求调研/问题分析）

### 1.1 用户视角现状
- 入口：点击 Worker 菜单 `📋 任务列表` 或输入 `/task_list`。
- 当前每条任务按钮会展示：`状态图标 + 任务类型图标 + 标题`（例：`🔍 🛠️ 修复登录问题`）。
- 痛点：任务类型图标信息密度低、视觉噪声高；在列表中更关注“状态+标题”，类型可在详情中查看。

### 1.2 需求范围
- 去除“任务列表/任务搜索结果”中每条任务按钮文本里的**任务类型图标**（包括未设置时的默认图标）。
- 保留任务按钮的**状态图标**（如 🔍🧪✅），并保持筛选/分页/详情跳转不变。

### 1.3 非需求范围（不改）
- 不改任务类型的编辑/选择体验（例如类型选择键盘仍可保留 emoji）。
- 不改任务详情页中“类型”的展示逻辑（仍以文本为主）。
- 不改任务数据来源、排序、分页、筛选规则。

### 1.4 验收标准（AC）
1. 任务列表与任务搜索结果中，每条任务按钮文本不再出现任务类型 emoji：`📌`/`🐞`/`🛠️`/`⚠️`，以及未设置时的 `⚪`。
2. 若任务存在状态，则状态图标仍显示在按钮最左侧；若无状态，则按钮直接显示标题。
3. 筛选/分页/搜索/进入任务详情等功能不受影响。
4. 相关单元测试通过。

### 1.5 方案（用户视角）
- 方案 A（最小改造 ✅ 推荐）：列表只展示 `状态图标 + 标题`，类型信息在详情中查看。
  - 优点：最简洁、改动小、列表阅读效率最高。
  - 缺点：列表中无法一眼区分类型（但可通过详情确认）。
- 方案 B：移除 emoji，但在标题前追加类型文字（如 `缺陷/需求/...`）。
  - 优点：仍保留类型信息。
  - 缺点：列表更长、更拥挤；中文标签重复度高。

---

## 2) design 阶段（开发设计 + 用例设计）
> YOLO：自动进入并执行。

### 2.1 影响面（技术视角定位）
- 任务列表与任务搜索结果：任务按钮文本的生成逻辑。
- 任务列表条目格式化（如存在独立的条目格式化函数）需同步去除类型图标，保持一致。

### 2.2 用例/Checklist（像测试一样）
- [x] 列表按钮：含状态时仅保留状态图标；不含状态时无任何前缀图标。
- [x] 搜索结果按钮：同列表按钮一致。
- [x] 任务类型选择键盘：不受影响。
- [x] 任务详情页“类型”：不受影响。
- [x] 回归：分页/筛选/搜索/详情跳转仍可用。

---

## 3) develop 阶段（需求开发/问题修复）
> YOLO：自动进入并执行；完成后自动回退到 vibe。

### 3.1 变更清单（待实现）
- [x] 去除任务列表/搜索结果按钮文本中的任务类型图标
- [x] 同步更新/补充相关单测断言
- [x] 运行测试并记录结果

### 3.2 自测记录
- 变更前：`.venv/bin/python -m pytest -q` → `533 passed`
- 变更后：`.venv/bin/python -m pytest -q` → `534 passed`

---

## 4) 参考资料（可验证来源）
- Telegram Bot API：InlineKeyboardButton 文本字段 `text` 作为按钮展示内容  
  https://core.telegram.org/bots/api#inlinekeyboardbutton
- aiogram 文档（InlineKeyboardMarkup/InlineKeyboardButton 相关）  
  https://docs.aiogram.dev/
