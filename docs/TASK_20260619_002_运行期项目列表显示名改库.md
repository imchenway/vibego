# TASK_20260619_002 运行期项目列表显示名改库

## 1. 背景与目标

用户提供 Telegram 项目列表截图，要求把运行期项目列表显示名改成截图中的名称，并保持大小写一致。

目标显示名：

| project_slug | 显示名 |
| --- | --- |
| `hyphafawnstudiobot` | `FawnStudio` |
| `vibegobot` | `Vibego` |
| `hyphaathenabot` | `Athena` |
| `cckgwmsappcore` | `WMS` |
| `zeus` | `Zeus` |
| `hyphamall` | `Mall` |
| `cckgwms2bot` | `WMS2` |
| `hyphagiraffebot` | `Giraffe` |
| `imchenwayblogbot` | `Blog` |
| `cckgpimbot` | `PIM` |

## 2. 受影响目录与边界

| 范围 | 是否影响 | 说明 |
| --- | --- | --- |
| `/Users/david/.config/vibego/config/master.db` | 是 | 更新 `projects.legacy_name`，这是 Master 当前运行期项目显示名来源。 |
| `/Users/david/.config/vibego/config/projects.json` | 是 | 同步更新 JSON 镜像中的 `name`，避免 DB/JSON 不一致。 |
| 仓库源码 | 否 | 本任务不改源码逻辑。 |
| SQLite schema | 否 | 不新增字段，不改表结构。 |
| worker 运行目录 / tmux session | 否 | `project_slug` 不变，运行路径不变。 |

## 3. 执行记录

### 3.1 改前只读确认

执行命令：

```bash
sqlite3 /Users/david/.config/vibego/config/master.db ".headers on" ".mode column" "SELECT id, bot_name, project_slug, legacy_name FROM projects ORDER BY bot_name COLLATE NOCASE;"
```

结果：10 个目标 `project_slug` 均存在，改前 `legacy_name` 均为空。

### 3.2 备份

已创建备份：

- DB：`/Users/david/.config/vibego/config/master.db.bak-TASK_20260619_002-20260619-104753`
- JSON：`/Users/david/.config/vibego/config/projects.json.bak-TASK_20260619_002-20260619-104753`

### 3.3 改库与同步 JSON

执行方式：使用 Python 脚本开启 SQLite `BEGIN IMMEDIATE` 事务，按 `project_slug` 更新 `projects.legacy_name`；随后同步更新 `projects.json` 的 `name` 字段，并使用临时文件原子替换 JSON。

## 4. 契约变更

- 运行期项目显示名现在来自 `legacy_name/name`，大小写按截图固定。
- `bot_name` 不变，仍表示 Telegram bot 用户名。
- `project_slug` 不变，避免影响日志目录、状态文件、worker 健康检查与 tmux session。

## 5. 验证矩阵

| 验证项 | 命令 | 预期 |
| --- | --- | --- |
| DB 显示名 | `SELECT project_slug, legacy_name FROM projects ...` | 10 个 slug 对应名称完全匹配。 |
| JSON 显示名 | Python 读取 `projects.json` | 10 个 `name` 完全匹配。 |
| 运行诊断 | `python3.11 -m vibego_cli doctor` | 通过，配置文件仍可读取。 |
| UI 刷新 | 重启 vibego master 后查看 Telegram 项目列表 | 显示截图同款名称。 |

## 6. 风险与回滚

- 风险：Master 如果已在运行，内存中的项目配置不会自动刷新，需要重启 master 后 UI 才能看到新名称。
- 风险：如果后续通过旧逻辑重导入 JSON，需确认 JSON 中 `name` 已同步保留。
- 回滚：停止 master 后，将备份文件复制回原路径：
  - `cp /Users/david/.config/vibego/config/master.db.bak-TASK_20260619_002-20260619-104753 /Users/david/.config/vibego/config/master.db`
  - `cp /Users/david/.config/vibego/config/projects.json.bak-TASK_20260619_002-20260619-104753 /Users/david/.config/vibego/config/projects.json`

## 7. 完成状态

- [x] 读取截图并确认目标显示名。
- [x] 只读确认 10 个目标项目存在。
- [x] 创建 DB 与 JSON 备份。
- [x] 更新 DB `legacy_name`。
- [x] 同步 JSON `name`。
- [x] 执行 DB/JSON/doctor 最终验证：`VERIFY_OK`，`python3.11 -m vibego_cli doctor` 成功。
- [ ] 等待用户重启 vibego master 后确认 UI。
