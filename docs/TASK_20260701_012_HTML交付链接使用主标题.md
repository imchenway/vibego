# TASK_20260701_012 HTML 交付链接使用主标题

## 背景

用户指出最终回复里的蓝色链接如果固定写成“打开 HTML”，信息密度低；更好的方式是使用 HTML 内部大标题（`<h1>`
）作为链接文字，例如“交付验收：移除阶段确认回退门禁”。本任务把该规则沉淀到全局 AGENTS 与 `vibe-diagram` skill。

## 变更范围

- `AGENTS-template.md`：在 HTML-first 与 HTML-only 交付规则中明确：Codex Markdown 链接文字必须使用 HTML 内部 `<h1>`
  主标题，不要写成固定的“打开 HTML”。
- `/Users/david/.config/vibego/agents/current/AGENTS-template.md`：通过同步保持与仓库模板一致。
- `vibego_cli/data/skills/vibe-diagram/SKILL.md`：更新交付铁律、文本压缩规则、HTML-only 信封示例与输出自检，示例改为语义标题链接。
- `tests/test_agents_template_migration.py`：补充全局模板回归断言。
- `tests/test_builtin_skills_injection.py`：补充 `vibe-diagram` 交付信封与文本压缩回归断言，反查固定链接文案。
- `AGENTS.md`：更新事实表中 HTML-only、文本压缩、Codex/Telegram 双端交付的证据口径。
- 本机全局目标：已通过 `agents-sync` 同步 `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、
  `/Users/david/.gemini/GEMINI.md`、`/Users/david/.config/vibego/AGENTS.md`，并同步 active skill 副本。

## TDD 记录

1. RED：先补测试，要求模板与 skill 都包含“链接文字必须使用 HTML 内部 `<h1>` 主标题”，且 `vibe-diagram` HTML-only 示例不再出现固定
   `[打开 HTML](file:///绝对路径/xxx.html)`。
2. 失败命令：
   `python3.11 -m pytest -q tests/test_agents_template_migration.py::test_agents_template_compresses_text_after_html_delivery tests/test_builtin_skills_injection.py::test_vibe_diagram_html_only_delivery_envelope_contract tests/test_builtin_skills_injection.py::test_vibe_diagram_delivery_reply_must_stay_concise`。
3. 失败原因：旧提示词只要求给 `file://` 链接，示例仍使用固定“打开 HTML”。
4. GREEN：更新 AGENTS 模板和 `vibe-diagram` 交付规则，使最终链接文字跟随 HTML `<h1>` 主标题。
5. 通过命令：同一聚焦命令 → `3 passed in 0.03s`。

## 验证

| 命令                                                                                                                                                                                                                                                                                                                    | 结果                                                                   | 说明                        |
|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------|---------------------------|
| `python3.11 -m pytest -q tests/test_agents_template_migration.py::test_agents_template_compresses_text_after_html_delivery tests/test_builtin_skills_injection.py::test_vibe_diagram_html_only_delivery_envelope_contract tests/test_builtin_skills_injection.py::test_vibe_diagram_delivery_reply_must_stay_concise` | `3 passed in 0.03s`                                                  | 聚焦回归通过                    |
| `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_builtin_skills_injection.py tests/test_agents_sync.py`                                                                                                                                                                                    | `75 passed in 0.85s`                                                 | 模板、skill、同步链路回归通过         |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                                                                                                                                                                                           | `ok: true`，4 个 target 为 `updated`                                    | 全局目标已同步                   |
| `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md`                                                                                                                                                                                                                             | `template current copy matches`                                      | active 模板一致               |
| `cmp -s vibego_cli/data/skills/vibe-diagram/SKILL.md /Users/david/.config/vibego/agents/current/vibego_cli/data/skills/vibe-diagram/SKILL.md`                                                                                                                                                                         | `skill current copy matches`                                         | active skill 一致           |
| `rg -n '链接文字必须使用 HTML 内部 \`<h1>\` 主标题\|不要写成固定的“打开 HTML”' ...`                                                                                                                                                                                                                                                         | 命中 active 模板、active skill 和 4 个全局目标                                  | 目标侧规则已落地                  |
| HTML 自检脚本                                                                                                                                                                                                                                                                                                             | `doctype/tokens/tabs/no_external_urls/no_candidate_prefix` 均为 `True` | 本交付 HTML 满足单文件与可访问 tab 基线 |
| `git diff --check -- ...`                                                                                                                                                                                                                                                                                             | 无输出，退出码 0                                                            | 本轮改动无空白错误                 |

## 影响与回滚

- 影响入口：后续 Codex HTML 交付信封的蓝色链接文字会变成 HTML 页面主标题，更像文档标题，而不是固定动作文案。
- Telegram 入口：仍以项目内 `.html/.htm` 文件附件为主交付；本次不改变附件发送链路。
- 用户待执行：无需再执行同步脚本；本轮已同步。若已运行的长期 worker 需要立刻使用新规约，请重启对应 worker。
- 回滚：恢复 `AGENTS-template.md` 与 `vibe-diagram/SKILL.md` 中的旧链接示例和旧断言，再执行 `agents-sync`。

## 追加修订：任务元信息 skill 条宽度对齐

用户在浏览器评审中指出顶部“本次使用的skill”条可以使用更合理的宽度，和下方主卡片最大宽度对齐。本轮追加修订：

- 当前 HTML：把 `.title-meta` 从 flex 改为 `display:grid; grid-template-columns:auto minmax(0,1fr); width:100%`，让
  `.skill-strip` 填满任务编码右侧剩余宽度。
- `vibe-diagram/SKILL.md`：明确标题顶部任务元信息宽度必须对齐页面主容器或下方主卡片最大宽度；`skill-strip` 必须填满剩余宽度，不得使用
  `max-width: min(760px,72vw)` 这类小于主容器的硬编码上限。
- 测试：`test_vibe_diagram_html_must_pin_task_handoff_meta_top_right` 已补充对应断言。

追加验证：

| 命令                                                                                                                            | 结果                                | 说明                       |
|-------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|--------------------------|
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_html_must_pin_task_handoff_meta_top_right` | `1 passed in 0.01s`               | RED 后 GREEN，skill 规则断言通过 |
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py`                                                              | `54 passed in 0.09s`              | `vibe-diagram` 规则回归通过    |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                   | `ok: true`，4 个 target 为 `updated` | active skill 和全局目标已同步    |

## 追加修订：移除顶部 skill 条与右上角交付卡片

用户在浏览器评审中明确要求移除两类生成内容：顶部“本次使用的 skill”长条，以及标题右侧交付信息卡片。本轮追加修订：

- 当前 HTML：删除顶部 skill 使用条，仅保留任务编码；删除标题右侧交付卡片，标题区改回单列主内容。
- `vibe-diagram/SKILL.md`：删除要求生成 skill 条和右上角交付卡片的正向提示；改为标题顶部只保留任务编码，交付动作、验证、风险和未覆盖点并入主图或正文区域。
- `tests/test_builtin_skills_injection.py`：把原“必须生成顶部 skill 条 / 右上角卡片”的断言改为“不得生成”。

追加验证：

| 命令                                                                                                                                                  | 结果                                | 说明                    |
|-----------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|-----------------------|
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_html_should_not_generate_skill_strip_or_top_right_handoff_cards` | `1 passed in 0.01s`               | RED 后 GREEN，移除规则断言通过  |
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py`                                                                                    | `54 passed in 0.08s`              | `vibe-diagram` 规则回归通过 |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                         | `ok: true`，4 个 target 为 `updated` | active skill 和全局目标已同步 |

## 追加修订：交付验收图收敛为单一验收账本

用户指出交付验收的四套切换图不够清晰易读。本轮把“交付验收图”的生成规则从四套并列视图收敛为一张可签收的验收账本：每条 R#
只保留 `要求 → 交付 → 证据 → 判定 → 动作` 的单行读序；R# 看板、证据热区、地铁线路只允许作为账本内局部表达，不再作为独立视图。

- `vibego_cli/data/skills/vibe-diagram/SKILL.md`：为交付验收图增加单账本例外，禁止四套候选图或 tab 切换。
- `vibego_cli/data/skills/vibe-diagram/references/delivery-acceptance.md`：改为 `验收账本 / 需求到证据签收表`
  ，声明“独立备选候选：无”。
- `vibego_cli/data/skills/vibe-diagram/references/page-mockup.md`：同步多图型清单中的交付验收口径。
- 当前 HTML：移除交付验收切换按钮、四个面板和切换脚本，改成单一 `acceptance-ledger` 签收表。
- `tests/test_builtin_skills_injection.py`：新增交付验收单账本规则与当前 HTML 结构反查；更新旧四视图断言。

追加验证：

| 命令                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 结果                                | 说明                                     |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|----------------------------------------|
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_candidate_atlas_mode_is_required_for_calibration tests/test_builtin_skills_injection.py::test_vibe_diagram_references_list_candidate_atlas_for_every_diagram_type tests/test_builtin_skills_injection.py::test_vibe_diagram_delivery_acceptance_uses_requirement_evidence_track tests/test_builtin_skills_injection.py::test_vibe_diagram_delivery_acceptance_uses_single_readable_ledger_not_four_candidate_tabs tests/test_builtin_skills_injection.py::test_task_20260701_012_delivery_html_uses_single_readable_acceptance_ledger` | `5 passed in 0.11s`               | RED 后 GREEN，交付验收单账本聚焦回归通过              |
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `56 passed in 0.10s`              | `vibe-diagram` 规则回归通过                  |
| `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_builtin_skills_injection.py tests/test_agents_sync.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `77 passed in 0.85s`              | 模板、skill、同步链路回归通过                      |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `ok: true`，4 个 target 为 `updated` | active skill、active reference 和全局目标已同步 |

## 追加修订：保留切换按钮并优化面板内部可读性

用户澄清：问题不是要移除四个按钮，而是四套交付验收图内部不够清晰易读。本轮撤回“单一账本例外”，改为保留切换入口，并把每个面板内部换成更清晰的布局图。

- 当前 HTML：恢复 `role="tablist"`、4 个 `role="tab"` 和 4 个 `role="tabpanel"`；按钮标签为“签收账本 / 证据泳道 /
  风险动作板 / 交付时间线”。
- 面板内部：每个面板都增加 `panel-summary` 主结论和 `reading-order` 阅读顺序，并分别使用 `acceptance-ledger`、
  `evidence-lanes`、`risk-action-board`、`delivery-timeline` 四种布局。
- `vibego_cli/data/skills/vibe-diagram/SKILL.md`：删除“交付验收图不生成 tab”的规则，改为“保留候选切换入口，不要用移除按钮或删除面板来解决可读性问题”。
- `vibego_cli/data/skills/vibe-diagram/references/delivery-acceptance.md`：恢复交付验收候选全集，备选改为更清晰的新布局：证据泳道图、风险动作板、交付时间线。
- `vibego_cli/data/skills/vibe-diagram/references/page-mockup.md`：同步多图型候选清单中的交付验收口径。
- `tests/test_builtin_skills_injection.py`：新增/调整断言，要求交付验收保留按钮并验证每个面板内部可读结构。

追加验证：

| 命令                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 结果                                | 说明                                     |
|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|----------------------------------------|
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_candidate_atlas_mode_is_required_for_calibration tests/test_builtin_skills_injection.py::test_vibe_diagram_references_list_candidate_atlas_for_every_diagram_type tests/test_builtin_skills_injection.py::test_vibe_diagram_delivery_acceptance_preserves_tabs_and_requires_readable_internal_layouts tests/test_builtin_skills_injection.py::test_task_20260701_012_delivery_html_preserves_tabs_with_readable_panel_layouts tests/test_builtin_skills_injection.py::test_vibe_diagram_delivery_acceptance_rejects_card_pile_sample` | RED：`5 failed`；GREEN：聚焦项通过        | 先证明旧规则和单账本 HTML 不满足用户澄清，再改规则与 HTML     |
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `56 passed in 0.08s`              | `vibe-diagram` 规则回归通过                  |
| `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_builtin_skills_injection.py tests/test_agents_sync.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `77 passed in 0.85s`              | 模板、skill、同步链路回归通过                      |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `ok: true`，4 个 target 为 `updated` | active skill、active reference 和全局目标已同步 |

## 追加修订：skill 提示词沉淀审查与精简

用户追问 skill 提示词是否已经全部沉淀，并要求沉淀后整体审查，在不影响逻辑的前提下精简。本轮执行结果：

- `vibego_cli/data/skills/vibe-diagram/SKILL.md`：保留近期反馈规则（主标题链接、禁止固定“打开 HTML”、标题顶部只保留任务编码、不要生成
  skill 使用清单、不生成右上角交付信息卡片、候选标题只显示图名、交付验收保留切换入口），同时合并重复描述；当前长度为 10286 字符。
- `vibego_cli/data/skills/vibe-diagram/references/delivery-acceptance.md`
  ：保留“不要用移除按钮或删除面板来解决可读性问题”、证据泳道图、风险动作板、交付时间线等交付验收规则；当前长度为 3088 字符。
- `tests/test_builtin_skills_injection.py`：新增 `test_vibe_diagram_prompt_compaction_preserves_recent_feedback_rules`
  ，用字符上限和关键规则反查防止后续提示词继续膨胀或误删近期沉淀。
- active 副本：已通过 `agents-sync` 同步，并用 `cmp` 反查仓库 skill 与 active skill/reference 一致。

追加验证：

| 命令                                                                                                                                                                                              | 结果                                | 说明                                     |
|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------|----------------------------------------|
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py::test_vibe_diagram_prompt_compaction_preserves_recent_feedback_rules`                                                           | `1 passed in 0.01s`               | 聚焦验证近期规则与精简阈值                          |
| `python3.11 -m pytest -q tests/test_builtin_skills_injection.py`                                                                                                                                | `57 passed in 0.07s`              | `vibe-diagram` 内置 skill 回归通过           |
| `python3.11 -m pytest -q tests/test_agents_template_migration.py tests/test_builtin_skills_injection.py tests/test_agents_sync.py`                                                              | `78 passed in 0.87s`              | 模板、skill、同步链路回归通过                      |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                                                                     | `ok: true`，4 个 target 为 `updated` | active skill、active reference 和全局目标已同步 |
| `cmp -s vibego_cli/data/skills/vibe-diagram/SKILL.md /Users/david/.config/vibego/agents/current/vibego_cli/data/skills/vibe-diagram/SKILL.md`                                                   | `SKILL cmp ok`                    | active skill 一致                        |
| `cmp -s vibego_cli/data/skills/vibe-diagram/references/delivery-acceptance.md /Users/david/.config/vibego/agents/current/vibego_cli/data/skills/vibe-diagram/references/delivery-acceptance.md` | `delivery reference cmp ok`       | active reference 一致                    |
| `git diff --check`                                                                                                                                                                              | 无输出，退出码 0                         | 空白检查通过                                 |
