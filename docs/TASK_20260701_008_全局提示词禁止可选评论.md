# TASK_20260701_008 全局提示词禁止可选评论

## 背景

用户要求在全局提示词里加入一条：`DO NOT send optional commentary`。

## 变更范围

- `AGENTS-template.md`：在 `## Hard boundaries` 下新增 `DO NOT send optional commentary`。
- `/Users/david/.config/vibego/agents/current/AGENTS-template.md`：同步仓库根模板，确保本机活跃模板包含同一约束。
- `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`：已同步受管全局目标
  `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、`/Users/david/.gemini/GEMINI.md`、
  `/Users/david/.config/vibego/AGENTS.md`。
- `tests/test_agents_template_migration.py`：新增 `test_agents_template_forbids_optional_commentary`，固定该全局提示词约束。

## 证据锚点

| 事项         | 证据                                                                                                                                                         |
|------------|------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 仓库根模板包含新约束 | `AGENTS-template.md`（锚点：`## Hard boundaries`、`DO NOT send optional commentary`）                                                                            |
| 活跃全局模板已同步  | `/Users/david/.config/vibego/agents/current/AGENTS-template.md`（锚点：`## Hard boundaries`、`DO NOT send optional commentary`）                                 |
| 受管全局目标已更新  | `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json` 输出 `ok: true`，`codex/claude/gemini/vibego` target 均为 `updated` |
| 测试固定新约束    | `tests/test_agents_template_migration.py`（锚点：`test_agents_template_forbids_optional_commentary`）                                                           |

## TDD 记录

1. RED：先新增 `tests/test_agents_template_migration.py::test_agents_template_forbids_optional_commentary`。
2. 失败命令：
   `python3.11 -m pytest -q tests/test_agents_template_migration.py::test_agents_template_forbids_optional_commentary`。
3. 失败原因：`AssertionError: assert 'DO NOT send optional commentary' in ...`。
4. GREEN：在仓库根模板与本机活跃模板 `Hard boundaries` 下新增同一句约束。
5. 通过命令：
   `python3.11 -m pytest -q tests/test_agents_template_migration.py::test_agents_template_forbids_optional_commentary` →
   `1 passed in 0.03s`。

## 验证

| 命令                                                                                                                  | 结果                                 | 说明                 |
|---------------------------------------------------------------------------------------------------------------------|------------------------------------|--------------------|
| `python3.11 -m pytest -q tests/test_agents_template_migration.py`                                                   | `14 passed in 0.03s`               | 模板迁移与约束测试通过        |
| `cmp -s AGENTS-template.md /Users/david/.config/vibego/agents/current/AGENTS-template.md && echo 'template synced'` | `template synced`                  | 仓库根模板与本机活跃模板一致     |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                         | `ok: true`，4 个 targets 为 `updated` | 受管全局 AGENTS 目标已同步  |
| `git diff --check -- AGENTS-template.md tests/test_agents_template_migration.py`                                    | exit 0，无输出                         | 当前代码与测试 diff 无空白错误 |

## 影响与回滚

- 影响入口：仅影响全局 AGENTS 模板规约与后续同步/注入出的提示词行为；无业务命令、Bot 指令或运行时流程变更。
- 用户待执行：本轮已执行 `agents-sync`；无需再执行同步脚本。已经运行中的长期 worker 是否热读取新规约待确认，如需立刻加载请按现有运维流程重启对应
  worker。
- 回滚：删除 `AGENTS-template.md` 与活跃模板中的 `DO NOT send optional commentary`，并删除
  `test_agents_template_forbids_optional_commentary`。
