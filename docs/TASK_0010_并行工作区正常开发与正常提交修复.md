# TASK_0010 并行工作区正常开发与正常提交修复

## 1. 背景

用户确认的目标：

1. 并行目录必须是可正常开发的真实工作区。
2. Codex 改动必须真实落入业务子仓库，而不是只形成根仓库空壳提交。
3. 当前分支场景下，源项目未提交改动要带入并行目录。
4. 有远端的仓库要能正常推送任务分支；无远端仓库不能再默认报 `origin` 错误。

仓库证据：

- `parallel_runtime.py`
  - 锚点：`prepare_parallel_workspace`
  - 旧行为：整目录复制后统一 `checkout -B <task_branch> <selected_ref>`，会把复制过来的脏工作区重置掉。
- `parallel_runtime.py`
  - 锚点：`commit_parallel_repos`
  - 旧行为：一律 `push_remote = repo.selected_remote or "origin"`。
- `parallel_runtime.py`
  - 锚点：`merge_parallel_repos`
  - 旧行为：一律 `fetch --all` + `(repo.selected_remote or "origin")`。

## 2. Class Impact Plan

### 2.1 受影响子项目与目录

- 并行运行时：`parallel_runtime.py`
- 测试：`tests/test_parallel_runtime.py`

### 2.2 具体受影响单元

1. `parallel_runtime.py`
   - `prepare_parallel_workspace`
   - `commit_parallel_repos`
   - `merge_parallel_repos`
   - 新增 Git 辅助判断函数（当前分支、状态路径、remote 解析、meta-only 判定）

### 2.3 实现文件与测试文件映射

| 单元 | 实现文件 | 测试文件 |
|---|---|---|
| 当前分支保留未提交改动 | `parallel_runtime.py` | `tests/test_parallel_runtime.py` |
| 非当前分支清理脏工作区 | `parallel_runtime.py` | `tests/test_parallel_runtime.py` |
| 父仓库 meta-only 提交跳过 | `parallel_runtime.py` | `tests/test_parallel_runtime.py` |
| 唯一 remote 自动推送 | `parallel_runtime.py` | `tests/test_parallel_runtime.py` |
| 无 remote 自动合并跳过 | `parallel_runtime.py` | `tests/test_parallel_runtime.py` |

### 2.4 直连依赖测试

- 本次仅纳入 `tests/test_parallel_runtime.py`
- 证据：
  - 该文件已直接覆盖 `prepare_parallel_workspace`
  - 新增测试也全部围绕 `parallel_runtime.py` 内 Git 运行时逻辑

### 2.5 测试范围升级判断

- 命中升级条件：❌ 否
- 原因：
  - 改动可稳定收敛在单个 runtime 模块
  - 不涉及数据库 schema、构建链、全局配置
  - 未修改 `bot.py` / 路由 / 持久化表结构

## 3. Baseline Gate

执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py
```

结果：

- ✅ `15 passed`

## 4. TDD 红灯

先补测试：

1. `test_prepare_parallel_workspace_preserves_dirty_changes_for_current_local_branch`
2. `test_prepare_parallel_workspace_drops_dirty_changes_for_other_branch`
3. `test_commit_parallel_repos_skips_meta_only_parent_and_keeps_real_child_commit`
4. `test_commit_parallel_repos_pushes_to_single_available_remote`
5. `test_merge_parallel_repos_skips_repo_without_remote`

首次执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py -k 'preserves_dirty_changes_for_current_local_branch or drops_dirty_changes_for_other_branch or skips_meta_only_parent_and_keeps_real_child_commit or pushes_to_single_available_remote or skips_repo_without_remote'
```

结果：

- ❌ `3 failed, 2 passed, 15 deselected`

失败点：

1. 非当前分支场景下，未提交改动仍被错误保留
2. 父仓库 meta-only 变化仍走到 `git commit`
3. 无 remote 自动合并仍默认走 `origin`

满足“先红后绿”。

## 5. 最小实现

文件：`parallel_runtime.py`

变更：

1. 新增 `_current_branch_name(...)`
   - 读取当前本地分支
2. 新增 `_extract_status_paths(...)`
   - 从 `git status --short` 解析变更路径
3. 新增 `_resolve_push_remote(...)`
   - 优先取 branch remote / `selected_remote` / 唯一 remote
4. 新增 `_direct_child_repo_names(...)` / `_is_meta_only_status(...)`
   - 识别父仓库仅包含子仓库指针/共享目录元信息变化的场景
5. 调整 `prepare_parallel_workspace(...)`
   - `selected_ref == HEAD` 或 “所选本地分支 == 当前分支” 时，使用 `checkout -b` 保留脏工作区
   - 其它基线分支场景在 `checkout -B` 后补 `reset --hard` + `clean -fd`
6. 调整 `commit_parallel_repos(...)`
   - meta-only 仓库直接 `skipped`
   - 无 remote 仓库改为“已本地提交，跳过推送”
7. 调整 `merge_parallel_repos(...)`
   - 无 remote 仓库直接 `skipped`
   - 不再默认回退 `origin`

## 6. Self-Test Gate

### 6.1 定向绿灯

执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py -k 'preserves_dirty_changes_for_current_local_branch or drops_dirty_changes_for_other_branch or skips_meta_only_parent_and_keeps_real_child_commit or pushes_to_single_available_remote or skips_repo_without_remote'
```

结果：

- ✅ `5 passed, 15 deselected`

### 6.2 最终两轮一致性回归

执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py
python3.11 -m pytest -q tests/test_parallel_runtime.py
```

结果：

- ✅ 第一轮：`20 passed`
- ✅ 第二轮：`20 passed`

### 6.3 未执行项说明

- 当前仓库未找到可证实的局部 typecheck / coverage 统一命令
- 证据：
  - `AGENTS.md`
  - 锚点：`当前覆盖率工具`
  - 锚点：`typecheck：TODO`

## 7. 用户可见结果

1. 当前分支场景下，新建并行目录会保留未提交改动。
2. 切到其它基线分支的新并行目录不会混入当前工作区脏改动。
3. 父仓库仅有嵌套 Git 指针变化时，不再生成误导性的空壳提交。
4. 真实业务子仓库即使未显式记录 `selected_remote`，只要仓库存在唯一 remote，仍可正常推送任务分支。
5. 无 remote 仓库不再报 `origin` 错误，会明确标记为本地提交/跳过自动合并。

## 8. 风险与回滚

### 8.1 风险

- 当前“meta-only” 判定基于：
  - 当前仓库下首层变更路径是否只落在嵌套仓库目录
  - 根仓库额外容忍 `docs` / `.idea`
- 若未来要把根仓库 `docs` 也作为正式交付物，需要单独细化判定规则。

### 8.2 回滚

- 回滚 `parallel_runtime.py` 中：
  - 当前分支保留脏工作区逻辑
  - meta-only 跳过逻辑
  - remote 解析逻辑
- 回滚 `tests/test_parallel_runtime.py` 五条新增测试

## 9. 补充修复：并行工作区生成 IDEA 多仓库 Git 映射

### 9.1 背景

用户继续反馈：

1. 直接用 IDEA 打开原始 `pim` 目录时，可以看到所有嵌套仓库分支。
2. 打开并行 `workspace` 目录时，只能看到根仓库分支，看不到全部子仓库分支。

仓库证据：

- 原始目录存在 `.idea/vcs.xml`
  - 路径：`/Users/david/cckg/tcapp/Back-End/pim/.idea/vcs.xml`
  - 锚点：`<component name="VcsDirectoryMappings">`
  - 内容包含 `tc-app-goods/...`、`web-base` 等 13 个 Git 目录映射
- 并行复制逻辑显式排除了 `.idea`
  - `parallel_runtime.py`
  - 锚点：`DEFAULT_PARALLEL_COPY_EXCLUDE_PATTERNS`

### 9.2 补充 Class Impact Plan

- 受影响单元：
  - `parallel_runtime.py`
    - 新增 `_write_workspace_idea_vcs_mappings(...)`
- 测试：
  - `tests/test_parallel_runtime.py`
    - `test_prepare_parallel_workspace_generates_idea_vcs_mappings_for_nested_repos`
    - 调整 `test_prepare_parallel_workspace_copies_full_workdir_and_excludes_generated_dirs`

### 9.3 Baseline Gate

执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py
```

结果：

- ✅ `20 passed`

### 9.4 TDD 红灯

执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py -k 'copies_full_workdir_and_excludes_generated_dirs or generates_idea_vcs_mappings_for_nested_repos'
```

结果：

- ❌ `2 failed, 19 deselected`

失败点：

1. 并行工作区未生成 `.idea/vcs.xml`
2. IDEA 无法像原始 `pim` 一样识别全部嵌套 Git 仓库

### 9.5 最小实现

文件：`parallel_runtime.py`

变更：

1. 新增 `_write_workspace_idea_vcs_mappings(...)`
   - 基于 `discover_git_repos(workspace_root, include_nested=True)` 生成并行工作区 `.idea/vcs.xml`
   - 自动写入：
     - `$PROJECT_DIR$`
     - `$PROJECT_DIR$/tc-app-goods/...`
     - `$PROJECT_DIR$/web-base`
2. 在 `prepare_parallel_workspace(...)` 中：
   - 整目录复制与共享 docs 链接完成后，自动补写 `.idea/vcs.xml`
3. 保持 `.idea/workspace.xml` 等源 IDE 噪音文件仍不复制

### 9.6 Self-Test Gate

执行：

```bash
python3.11 -m pytest -q tests/test_parallel_runtime.py -k 'copies_full_workdir_and_excludes_generated_dirs or generates_idea_vcs_mappings_for_nested_repos'
python3.11 -m pytest -q tests/test_parallel_runtime.py
python3.11 -m pytest -q tests/test_parallel_runtime.py
```

结果：

- ✅ 定向：`2 passed, 19 deselected`
- ✅ 第一轮全量：`21 passed`
- ✅ 第二轮全量：`21 passed`

### 9.7 用户可见结果

1. 并行工作区仍不会复制原始 `.idea/workspace.xml` 等 IDE 噪音文件。
2. 但会自动生成 `.idea/vcs.xml`。
3. 用 IDEA 直接打开并行 `workspace` 时，可以像原始 `pim` 一样识别全部嵌套 Git 仓库并查看分支。
