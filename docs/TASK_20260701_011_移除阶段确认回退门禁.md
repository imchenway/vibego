# TASK_20260701_011 移除阶段确认回退门禁

## 背景

用户要求把提示词里“确认”和“阶段回退”相关逻辑去掉。确认范围：只删除阶段门禁、确认复用、任务闭环后回到交互起点；保留高风险变更确认、Product
Design brief、证据不足写待确认等非阶段门禁内容。

## 变更范围

- `AGENTS-template.md`：
    - 将 `superpowers:brainstorming` 路由从“任何任务进入下一阶段前必须先做并等待确认”收敛为“需求、方案、行为变更、复杂设计：使用”。
    - 删除 Work contract 中三条阶段门禁：阶段确认、同目标确认复用、修改类动作闭环后任务边界复位。
- `/Users/david/.config/vibego/agents/current/AGENTS-template.md`：同步仓库模板。
- `tests/test_agents_template_migration.py`：把回归口径改为禁止上述阶段门禁残留。
- 本机全局目标：已通过 `agents-sync` 同步 `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、
  `/Users/david/.gemini/GEMINI.md`、`/Users/david/.config/vibego/AGENTS.md`。

## TDD 记录

1. RED：先修改 `test_agents_template_removes_brainstorming_stage_gate_and_task_reset`，要求模板保留简单 brainstorming
   路由但不包含阶段确认/确认复用/任务回退文本。
2. 失败命令：
   `python3.11 -m pytest -q tests/test_agents_template_migration.py::test_agents_template_removes_brainstorming_stage_gate_and_task_reset`。
3. 失败原因：旧模板仍包含“任何任务进入下一阶段前必须...”“只有用户明确确认...”“不得把上一任务的确认跨任务复用”等文本。
4. GREEN：更新模板并同步活跃模板。
5. 通过命令：`python3.11 -m pytest -q tests/test_agents_template_migration.py` → `14 passed in 0.03s`。

## 验证

| 命令                                                                                          | 结果                                | 说明             |
|---------------------------------------------------------------------------------------------|-----------------------------------|----------------|
| `python3.11 -m pytest -q tests/test_agents_template_migration.py`                           | `14 passed in 0.03s`              | 模板迁移与提示词约束回归通过 |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json` | `ok: true`，4 个 target 为 `updated` | 本机全局目标已同步      |

## 影响与回滚

- 影响入口：后续全局 AGENTS 规约不再要求每次进入下一阶段先做确认，也不再要求交付后回到新的交互起点。
- 保留项：高风险新增依赖/构建/CI 仍需征得确认；Product Design brief 相关确认仍保留；证据不足仍写“待确认/推断”。
- 用户待执行：无需再执行同步脚本；本轮已同步。已运行长期 worker 如需立刻使用新规约，请重启对应 worker。
- 回滚：恢复 `AGENTS-template.md` 的三条阶段门禁，恢复测试断言，并重新执行 `agents-sync`。
