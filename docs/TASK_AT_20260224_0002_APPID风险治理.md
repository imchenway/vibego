# TASK_AT_20260224_0002 APPID 风险治理记录

## 1. 背景

- 发现字符串 `wx[REDACTED]` 被 GitHub Secret Scanning 识别为 `tencent_wechat_api_app_id` 风险。
- 用户决策：
    - 范围：所有分支 + 所有 tags（2C）
    - 执行：由助手直接执行（2A）

## 2. 执行时间

- 2026-02-24

## 3. 执行动作

### 3.1 历史改写与脱敏

- 安装并使用 `git-filter-repo`。
- 将历史与当前中的值统一替换：
    - `wx[REDACTED]` -> `wx_APPID_REDACTED`

执行命令（核心）：

```bash
git filter-repo --force --replace-text <(echo 'wx[REDACTED]==>wx_APPID_REDACTED')
```

### 3.2 远端同步

- 由于历史被改写，已执行强推：

```bash
git push origin --force --all
git push origin --force --tags
```

## 4. 验证结果

### 4.1 工作区与历史排查

```bash
rg -n "wx[REDACTED]" .
git log --all -S "wx[REDACTED]" --oneline | wc -l
git rev-list --objects --all | grep -F "wx[REDACTED]" | wc -l
```

结果：均为 0 / 无命中。

### 4.2 受影响测试回归

```bash
PYTHONPATH=. pytest -q tests/test_wx_preview_port_flow.py
```

结果：`29 passed`

## 5. 剩余事项

- GitHub Secret Scanning 告警需在仓库安全页面手动标记为已处理（当前环境无 `gh` 与 GitHub Token）。

官方参考：

- Supported
  patterns: https://docs.github.com/en/code-security/reference/secret-security/supported-secret-scanning-patterns
- Resolving
  alerts: https://docs.github.com/en/code-security/secret-scanning/managing-alerts-from-secret-scanning/resolving-alerts
- Removing sensitive
  data: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository
