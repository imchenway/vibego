# TASK_20260701_009 内置 Skill 同步改为索引注入

## 背景

用户指出 Vibego 当前把 `vibego_cli/data/skills/*/SKILL.md` 全文注入 AGENTS 的实现是错误的，并要求参考 superpowers：skill
应通过注册/索引稳定触发，正文按需读取，而不是常驻塞进全局提示词。

## 变更范围

- `scripts/models/common.sh`：`sync_agents_block` 内置 Python 渲染逻辑改为提取 `SKILL.md` frontmatter 索引，只注入 `name`、
  `description`、`vibego-skill-source` 路径和“命中后读取全文 / 失败 fail-closed”说明。
- `vibego_cli/agents_sync.py`：CLI/Master 同步服务使用同样的索引注入口径。
- `tests/test_builtin_skills_injection.py`：将同步测试从“必须包含大段 vibe-diagram
  正文”改为“只包含索引且不得包含正文规则 / reference 路径 / CSS token”。
- `tests/test_agents_sync.py`：覆盖 Python 同步服务不再把测试用 `# 图形协议` 正文注入目标 AGENTS。
- `AGENTS.md`：更新事实表，记录当前是索引注入而不是全文注入。
- 本机全局目标：已通过 `agents-sync` 更新 `/Users/david/.codex/AGENTS.md`、`/Users/david/.claude/CLAUDE.md`、
  `/Users/david/.gemini/GEMINI.md`、`/Users/david/.config/vibego/AGENTS.md`。

## 证据锚点

| 事项                     | 证据                                                                                                                 |
|------------------------|--------------------------------------------------------------------------------------------------------------------|
| Shell worker 启动同步只注入索引 | `scripts/models/common.sh`（锚点：`extract_skill_index`、`AGENTS 只保留索引`）                                                |
| CLI/Master 同步只注入索引     | `vibego_cli/agents_sync.py`（锚点：`_extract_skill_index`、`AGENTS 只保留索引`）                                              |
| Shell 同步测试固定不注入正文      | `tests/test_builtin_skills_injection.py`（锚点：`test_sync_agents_block_embeds_builtin_vibe_diagram_skill_index_only`） |
| Python 同步测试固定不注入正文     | `tests/test_agents_sync.py`（锚点：`test_sync_agents_writes_override_and_targets`）                                     |
| 仓库事实表更新                | `AGENTS.md`（锚点：`Vibego 内置 Skills 索引注入约束`）                                                                          |

## TDD 记录

1. RED：先修改 `tests/test_agents_sync.py::test_sync_agents_writes_override_and_targets` 和
   `tests/test_builtin_skills_injection.py::test_sync_agents_block_embeds_builtin_vibe_diagram_skill_index_only`
   ，要求输出包含索引说明且不包含正文规则。
2. 失败命令：
   `python3.11 -m pytest -q tests/test_agents_sync.py::test_sync_agents_writes_override_and_targets tests/test_builtin_skills_injection.py::test_sync_agents_block_embeds_builtin_vibe_diagram_skill_index_only`。
3. 失败原因：旧实现仍把 `SKILL.md` 正文拼入 AGENTS，缺少“命中后读取全文”的索引说明。
4. GREEN：实现 frontmatter 索引提取，替换 shell 与 Python 两条同步链路的正文拼接。
5. 通过命令：同一聚焦命令 → `2 passed in 0.16s`。

## 验证

| 命令                                                                                                                                                                                                            | 结果                                                                                                                            | 说明                 |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------|--------------------|
| `python3.11 -m pytest -q tests/test_agents_sync.py::test_sync_agents_writes_override_and_targets tests/test_builtin_skills_injection.py::test_sync_agents_block_embeds_builtin_vibe_diagram_skill_index_only` | `2 passed in 0.16s`                                                                                                           | TDD 聚焦测试通过         |
| `python3.11 -m pytest -q tests/test_agents_sync.py tests/test_builtin_skills_injection.py`                                                                                                                    | `60 passed in 0.88s`                                                                                                          | 同步与内置 skill 相关回归通过 |
| `python3.11 -m vibego_cli agents-sync --source-root /Users/david/hypha/tools/vibego --json`                                                                                                                   | `ok: true`，4 个 target 为 `updated`                                                                                             | 本机全局目标已改为短索引       |
| `rg` 检查 4 个全局目标                                                                                                                                                                                               | 只保留 `vibego-skill-source`、`description` 和按需读取说明；未命中 `## 自动路由规则`、`卡片堆积不是图`、`--paper: #fbfdff`、`references/system-architecture` | 确认大段正文未再常驻注入       |

## 影响与回滚

- 影响入口：worker 正常重启、`start_tmux_codex.sh`、`vibego agents-sync`、Master AGENTS 同步按钮产出的 AGENTS 受管块。
- 用户待执行：本轮已同步本机全局目标；无需再执行同步脚本。已运行的长期 worker 如需立刻使用新 AGENTS，仍需按现有流程重启。
- 回滚：恢复 `render_builtin_skills` 为读取 `SKILL.md` 全文，恢复测试中对正文规则的断言，并重新执行 `agents-sync`。
