# TASK_20260304_004 Master 代理抖动下回调超时防护

## 背景

- 现网出现 master 项目列表/按钮偶发“无响应”。
- 日志证据：`~/.config/vibego/logs/vibe.log` 存在大量 `Proxy connection timed out: 60` 与 `query is too old`。

## 目标

1. 按钮耗时操作先应答 callback，避免回调超时。
2. 项目概览发送遇到瞬时网络异常时进行有限重试，降低偶发失败率。

## 影响面

- 代码：`master.py`
- 测试：`tests/test_master_network_resilience.py`

## TDD 过程

### 先写测试（失败）

- `tests/test_master_network_resilience.py::test_run_action_ack_before_run_worker`
- `tests/test_master_network_resilience.py::test_send_projects_overview_retries_on_transient_network_error`
- 首次执行：`2 failed`

### 实现

- 新增 `_send_message_with_retry`（有限重试 + 退避）
- `on_project_action` 中 `run/start_all/stop_all/stop` 改为先安全答复 callback 再执行耗时逻辑
- 异常路径下 callback 答复改为安全模式，避免二次异常覆盖主错误

### 回归

- 受影响测试、全量测试、build 双轮通过

## 风险

- 回调文案时序变化（由“操作后答复”改为“操作前答复”）。

## 回滚点

- 回滚文件：`master.py`、`tests/test_master_network_resilience.py`
- 回滚方式：撤销以上两文件本次变更即可恢复旧行为。
